{
  "name": "azurestorage-gui",
  "version": "1.0.0",
  "description": "GUI Interface for Microsoft AzureStorage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify -v -d -t lessify -t [ babelify --presets [ es2015 react ] ] index.js -o compiled.js",
    "build": "NODE_ENV=production browserify index.js -t lessify -t [ babelify --presets [ es2015 react ] ] | uglifyjs > compiled.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gurinderhans/azurestorage-gui"
  },
  "author": "gurinderhans",
  "license": "MIT",
  "dependencies": {
    "azure-storage": "^0.10.0",
    "react": "^15.0.1",
    "react-dom": "^15.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "lessify": "^1.0.1",
    "uglify-js": "^2.6.2",
    "watchify": "^3.7.0"
  }
}
